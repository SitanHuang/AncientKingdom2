{"data":[[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null],[{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null],[{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null],[{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null],[{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null],[{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null,null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null],[{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null,null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null],[null,null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":null,"type":null},null,null],[null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null],[null,null,null,null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null],[null,null,null,null,null,{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Green","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Green","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Yellow","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Yellow","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Yellow","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null],[null,null,null,{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null],[null,null,null,null,null,null,null,null,null,{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Wheat","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":null,"type":null},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null],[null,{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Black","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Brown","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Brown","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null],[{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Black","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null],[null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null],[null,{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null,null],[null,null,null,{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null,null,null],[null,null,null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null],[null,null,null,{"color":"Blue","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null],[null,null,{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null],[null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null],[null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null],[null,null,null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null],[null,null,null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null],[null,null,null,null,{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,{"color":"China","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"China","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,null,null,null,null,{"color":"Blue","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Blue","type":{"defend":35,"income":function (civ) {
            civ.politic += Math.random();
            civ.money += Math.random() * 35;
            civ.happiness += 0.55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("经", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},null,null,null,null,null,null,null,null,null,null,null,null,{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"Purple","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"defend":25,"income":function (civ) {
            civ.politic += Math.random() / 4;
            civ.money += Math.random() * 7;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("市", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        }}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic += Math.random() * 10;
            civ.money += Math.random() * 55;
        },"draw":function (x, y) {
            var context = canvas.getContext('2d');
            context.font = BLOCK_SIZE - 1 + "px 'Roboto Mono'";
            context.fillStyle = civs[data[y][x].color].fontColor;
            context.fillText("京", x * BLOCK_SIZE, y * BLOCK_SIZE + BLOCK_SIZE);
        },"defend":55}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},{"color":"White","type":{"income":function (civ) {
            civ.politic -= Math.random() / 55;
            civ.money += Math.random() / 2;
        },"draw":function () {
        },"defend":1}},null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null]],"civs":{"China":{"color":"#DB4437","fontColor":"#ffffff","money":3584.9,"technology":9,"politic":45.9,"birth":[25,40],"military":0,"years":0.5,"deposit":1.1,"happiness":103.71999999999989,"income":1202.47,"expense":45.9,"oldMoney":2428.33,"newMoney":3630.800570472222,"ii":459,"urban":46.41,"cityCount":213,"logistics":0,"war":{"Brown":-30,"Yellow":-80,"Blue":-40,"Purple":-40,"Black":-40},"ai":true},"Blue":{"color":"#90caf9","fontColor":"#000000","money":466.24,"technology":1,"politic":30,"military":0,"years":0.5,"deposit":1.1,"happiness":104.39999999999998,"income":254.73,"expense":11,"oldMoney":222.51,"newMoney":477.235017486284,"ii":110,"urban":26.36,"cityCount":29,"logistics":0,"birth":[41,4],"ai":true},"Green":{"color":"#52b848","fontColor":"#000000","money":1516.38,"technology":10,"politic":30,"birth":[6,23],"military":0,"years":0.5,"deposit":1.1,"happiness":87.68999999999993,"income":563.48,"expense":14.7,"oldMoney":967.6,"newMoney":1531.083774662339,"ii":147,"urban":57.14,"cityCount":84,"logistics":0,"war":{"Brown":-15},"ai":true},"Black":{"color":"#000000","fontColor":"#ffffff","money":90.37,"technology":1,"politic":21.67148,"military":0,"years":0.5,"deposit":1.1,"happiness":100.55,"income":58.35,"expense":10.7,"oldMoney":42.72,"newMoney":101.06933629434894,"ii":107,"urban":4.67,"cityCount":5,"logistics":0,"birth":[28,0],"ai":true},"Yellow":{"color":"#ffe067","fontColor":"#000000","money":773.14,"technology":9,"politic":21.69256,"birth":[11,46],"military":0,"years":0.5,"deposit":1.1,"happiness":65.46,"income":129.9,"expense":3.2,"oldMoney":646.44,"newMoney":776.3421277047629,"ii":32,"urban":71.88,"cityCount":23,"logistics":0,"war":{"Brown":-30},"ai":true},"Purple":{"color":"#950095","fontColor":"#ffffff","money":140.16,"technology":1,"politic":9.62064,"birth":[43,20],"military":0,"years":0.5,"deposit":1.1,"happiness":61.18,"income":52.11,"expense":8.8,"oldMoney":96.85,"newMoney":148.9609882665821,"ii":88,"urban":14.77,"cityCount":13,"logistics":0,"ai":true},"White":{"color":"#f0f0f0","fontColor":"#000000","money":89.42,"technology":1,"politic":16.287679999999998,"birth":[47,23],"military":0,"years":0.5,"deposit":1.1,"happiness":61.04,"income":36.47,"expense":2.2,"oldMoney":55.15,"newMoney":91.61598783222291,"ii":22,"urban":13.64,"cityCount":3,"logistics":0,"ai":true},"Brown":{"color":"brown","fontColor":"#ffffff","money":1752.94,"technology":1,"politic":30,"military":0,"years":0.75,"deposit":1.1,"happiness":115.94999999999992,"income":811.35,"expense":30,"oldMoney":971.59,"newMoney":1782.9377959225023,"ii":300,"urban":45,"cityCount":135,"logistics":0,"birth":[13,30],"ai":true},"Wheat":{"color":"wheat","fontColor":"#000000","money":1000,"technology":1,"politic":21.83928,"military":0,"years":0.5,"deposit":1.1,"happiness":100,"income":99.31,"expense":13,"oldMoney":91.01,"newMoney":190.32145801198126,"ii":130,"urban":6.15,"cityCount":8,"logistics":0,"birth":[13,14],"ai":true}}}